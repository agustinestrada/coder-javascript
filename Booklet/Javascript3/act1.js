let texto = prompt('ingrese un texto')
let repeticiones = prompt('ingrese un numero')

function bartAlPizarron (texto, repeticiones) {
    for (let i = 1; i <= repeticiones; i++) {;
        console.log(texto)
    }
}

bartAlPizarron()
